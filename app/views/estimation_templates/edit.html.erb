<h1><%=Workpackage.find_by_id(@estimation_template.workpackage_id).name%></h1>

<%= form_for(@estimation_template) do |f| %>
  <% if @estimation_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estimation_template.errors.count, "error") %> prohibited this estimation_template from being saved:</h2>

      <ul>
      <% @estimation_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label "Kommentar:" %><br>
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', FormTemplate.find_by_id(@estimation_template.formTemplate_id)%>
