<%= form_for(@milestone) do |f| %>
  <% if @milestone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone.errors.count, "error") %> prohibited this milestone from being saved:</h2>

      <ul>
      <% @milestone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workpackage_id %><br>
    <%= f.number_field :workpackage_id %>
  </div>
  <div class="field">
    <%= f.label :endtime %><br>
    <%= f.number_field :endtime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
