

<h1>
  <%= "Projekt: " + @project.name %>
  <%= link_to 'Projekt löschen', @project, method: :delete, data: { confirm: 'Are you sure?' }, :id=> 'btn', :class=> 'btn btn-sm btn-danger pull-right'%>
</h1>

<table class="table">
  <tbody>
    <tr>
      <td><%= link_to 'PBS', ProductBreakdownStructure.find_by(p_id: @project.id) %></td>
      <td><%= link_to 'WBS', WorkBreakdownStructure.find_by(p_id: @project.id) %></td>
      <td><%= link_to 'RBS', ResourceBreakdownStructure.find_by(p_id: @project.id) %></td>
      <td><%= link_to 'RAM', ResourceAllocationMatrix.find_by(p_id: @project.id) %></td>
      <td><%= link_to 'Roadmap', Roadmap.find_by(p_id: @project.id) %></td>
      <td><%= link_to 'Delphi', DelphiEstimation.find_by(p_id: @project.id) %></td>
    </tr>
  </tbody>
</table>

