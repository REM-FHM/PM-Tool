<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tNumber %><br>
    <%= f.number_field :tNumber %>
  </div>
  <div class="field">
    <%= f.label :tName %><br>
    <%= f.text_field :tName %>
  </div>
  <div class="field">
    <%= f.label :wbs_id %><br>
    <%= f.number_field :wbs_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
