<h2>Bitte tragen Sie Ihren Namen ein</h2>

<%= form_for(@expert_form) do |f| %>
  <% if @expert_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expert_form.errors.count, "error") %> prohibited this expert_form from being saved:</h2>

      <ul>
      <% @expert_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class = "form-inline">
          <div class="form-group">
 <%= f.hidden_field :round_id, :value => @expert_form.round_id %>
  <div class="field">
    <%= f.text_field :expertName %>
  </div>
</div>

  <div class="form-group">
    <div class="actions">
    <%= f.submit "Weiter zur Schätzung", class: "btn btn-xs btn-success" %>
    </div>
  </div>
</div>
<% end %>

<%= link_to 'Zurück zur Rundenübersicht', expert_forms_path, class: "btn btn-xs btn-success" %>
